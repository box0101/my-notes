# Git

以个人目前掌握知识来说，Git最鲜明的是**集中式工作流**和**功能分支工作流**。前者是实现多人共同开发，后者则是实现按功能对代码提交进行区分。按开发者不同、开发的功能不同，使得代码提交历史变得更加有序可控，让多人开发下的代码仓库更加有保障。



#### 集中式工作流

当多人同时开发时，各自克隆一份仓库的代码到自己本地，修改完成后将修改记录提到暂存区，再push到仓库里。

既然是多人开发那么就不可避免地会出现冲突，当同一个地方的代码与仓库出现了冲突，会被拒绝push，需要解决冲突后再提交。

解决冲突一般流程：

git status查看状态，可以得到冲突文件，接着在冲突文件中手动解决冲突，保留自己想要的，再add、commit标记冲突已解决，



Git分支并不像我一开始所认为的只有Master分支和开发者创建的功能分支，一个具有较良好健壮性的代码仓库包括Master分支、功能集成develop分支、功能分支feature、介于Master和develop中间的发布分支release，还有维护分支hotfix。其中release和hotfix分支是为了满足当下的发布/修复bug而出现的，当完成发布/bug修复后应该删除分支，下次需要时再从Master拉出来。
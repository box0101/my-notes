# pnpm VS npm

面试常问二者区别、为什么 pnpm 比 npm 快。



#### npm 的工作机制

1. 安装包时会把每个包完整下载到 `node_modules`。
2. 对依赖的依赖也会重复下载和存放，如果不同版本共存就会有很多层 `node_modules`。
3. 每个项目的依赖都是“项目私有”，所以即使全局有相同的包也还是会重新安装一次。

所以**磁盘占用大**，因为每个项目独立安装依赖；**安装慢**，尤其是依赖树复杂的项目，每次都需要解析、下载、写入文件。



#### pnpm 的工作机制